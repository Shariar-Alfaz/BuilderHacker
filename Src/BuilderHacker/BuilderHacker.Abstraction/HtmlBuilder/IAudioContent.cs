namespace BuilderHacker.Abstraction.HtmlBuilder
{
    /// <summary>
    /// Represents audio content within an HTML document structure.
    /// </summary>
    /// <remarks>This interface is intended for types that encapsulate audio elements as part of an HTML node
    /// hierarchy. Implementations typically correspond to audio tags or embedded audio resources in HTML
    /// content.</remarks>
    public interface IAudioContent : IHtmlNode
    {
    }
}
